frontend Run
      cd bikezone-frontend
      npm run dev

Git commands
      git add .
      git commit -m "commit-message"
      git push ( only for current branch or git push origin branch-name  )
      git checkout -b branch-name
